# TD Realty Seller Intelligence - Environment Variables
# Copy this file to .env and fill in your values

# Google Apps Script Web App URL
# Get this after deploying the Apps Script (see google-apps-script/Code.gs)
# Example: https://script.google.com/macros/s/AKfycbw.../exec
APPS_SCRIPT_URL=https://script.google.com/macros/s/YOUR_DEPLOYMENT_ID/exec

# Auth Token for the Apps Script
# This must match the AUTH_TOKEN constant in your deployed Apps Script
# Generate a random string for security (e.g., use: openssl rand -hex 32)
APPS_SCRIPT_TOKEN=your_random_auth_token_here

# Optional: Override default rate limiting (requests per second)
# RATE_LIMIT_PER_SECOND=1.0

# Optional: Enable debug logging
# DEBUG=true
